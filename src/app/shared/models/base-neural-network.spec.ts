import { BaseNeuralNetwork } from './base-neural-network';

describe('BaseNeuralNetwork', () => {
  it('should create an instance', () => {
    expect(new BaseNeuralNetwork()).toBeTruthy();
  });
});
